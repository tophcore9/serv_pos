#ifndef SHOWDISHFORM_H
#define SHOWDISHFORM_H

#include <QDialog>
#include <QtWidgets>

#include <QSqlTableModel>

class ShowDishForm : public QDialog
{
    Q_OBJECT
public:
    explicit ShowDishForm(QSqlTableModel *dish_model, QSqlTableModel *category_model, QWidget *parent = 0);

signals:

public slots:
};

#endif // SHOWDISHFORM_H